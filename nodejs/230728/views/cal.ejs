<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <% for (var i = 0; i < 8; i++) { %> <% for (var j = 0; j < 9; j++) { %>
    <div><%= data[i] %> * <%= j + 1 %> = <%= data[i] * (j + 1) %></div>
    <% } %> <% } %>
  </body>
</html>
